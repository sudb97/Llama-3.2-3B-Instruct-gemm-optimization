# Copy to container.env or pass on the command line.
# Already inside the profiler container:
# CONTAINER=local
# CONTAINER=8c24291250f3
# From the host, docker exec into a running container:
# CONTAINER=cuda-profiler-dev
# OPT_LOOP_EXEC=docker
